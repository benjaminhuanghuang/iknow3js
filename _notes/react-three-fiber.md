# React + react-three-fiber

```sh
npm install three @react-three/fiber @react-three/drei
```

- three: core 3D engine
- @react-three/fiber: React renderer
- @react-three/drei: useful helpers (camera, lights, controls, loaders)
